[project]
name = "ogd-integrated-view"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.28.1",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "streamlit>=1.59.2",
]

[project.scripts]
ogd-integrated-view = "ogd_integrated_view.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
